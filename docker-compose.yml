services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_USER: uplift
      POSTGRES_PASSWORD: uplift
      POSTGRES_DB: uplift
    ports:
      - '5432:5432'
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U uplift']
      interval: 5s
      retries: 10

  redis:
    image: redis:7-alpine
    ports:
      - '6379:6379'

volumes:
  pgdata:
